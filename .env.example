# The anon key is compiled into the bundle. That is expected and safe only because RLS
# is enabled on every table. Never put the service role key here.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
